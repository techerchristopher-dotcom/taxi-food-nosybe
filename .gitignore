# Dépendances
node_modules/

# Build
dist/
build/
.vite/

# Environnement
.env
.env.local
.env.*.local

# Éditeurs / OS
.DS_Store
.vscode/
.idea/
*.log

# Divers
.cache/

# ⚠️ SECRETS — ne JAMAIS committer
# La clé de compte de service Firebase Admin SDK donne un accès complet au projet
# Firebase (et donc à l'envoi de notifications au nom de Taxi Food). Elle a été
# trouvée le 2026-08-21 posée dans « taxi food/ », à l'intérieur du dépôt, non
# suivie mais non ignorée : un seul `git add .` l'aurait publiée sur GitHub.
# Elle vit dans le coffre-fort d'identifiants EAS, pas dans le dépôt.
*firebase-adminsdk*.json
taxifoodnosybe-*.json
*service_role*
*-service-account*.json

# Dossiers de travail personnels déposés dans le dépôt (devis, factures, exports,
# zips de maquettes). Rien à versionner ici, et ça évite d'y committer un secret
# par ricochet.
taxi food/
motion design/
motion design */
image/
*.zip
.netlify/

# Dossiers d'images sources deposes par le porteur du projet, hors depot comme
# « motion design/ » et « image/ » plus haut. Les visuels qui comptent vivent
# dans le Storage Supabase (buckets `produits`, `logo`, `MENU`), qui fait foi ;
# ces dossiers ne sont que les originaux de travail.
#   partenaire /sicilien /visuels/  -> 41 PNG, 64 Mo, deposes dans
#                                      produits/les-siciliens/ le 2026-09-05
#   partenaire /sicilien /*.JPG     -> photos de la CARTE PAPIER (pas du lieu)
partenaire /
carte de visite flyer 1/

# Secrets de travail, hors depot par construction (cle d'API n8n, jetons de test).
# Ne JAMAIS committer : voir docs/N8N-NOTIFICATIONS.md pour ce qui doit s'y trouver.
.secrets.local
